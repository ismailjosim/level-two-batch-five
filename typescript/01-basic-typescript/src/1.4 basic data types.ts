// * Primitive data types
// string
const firstName: string = 'ismail'

// number
const roll: number = 50

// boolean
const isAdmin: boolean = false

// undefined
const working: undefined = undefined

// null
const maritalStatus: null = null

//* Non primitive data types
const friends: string[] = ['ismail', 'hossen', 'josim']
const friendsContact: number[] = [1, 2, 3, 4]

// * Tuple => special types of Array [we can defined type of values]
const personInfo: [string, number, boolean] = ['MR. X', 20, true]

// object types
const user: {
	fullName: string
	age: number
	isMarried: boolean
} = {
	fullName: 'ismail',
	age: 26,
	isMarried: false,
}
